# AI 调用模式：bff（生产默认，经 BFF 转发）/ direct（仅开发调试，Key 由用户自填，有泄露风险）
VITE_AI_MODE=bff
# BFF 或直连的 baseURL（BFF 模式通常为同源 /api/ai 代理）
VITE_AI_BASE_URL=/api/ai
# 构建注入的应用版本号（展示于设置页与错误日志）
VITE_APP_VERSION=0.1.0

# ⚠️ AI_VENDOR_API_KEY 仅允许在 BFF 运行环境使用，严禁加 VITE_ 前缀下发浏览器。
